menuentry "Boot an exising 32bit Linux OS installed on the disk" {
  linux  /isolinux/rescuecd scandelay=1 root=auto
  initrd /isolinux/initram.igz
}

menuentry "Boot a 32bit or 64bit Linux OS installed on the disk" {
  linux  /isolinux/rescue64 scandelay=1 root=auto
  initrd /isolinux/initram.igz
}
